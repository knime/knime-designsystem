import type { KdsInputFieldProps } from "../types";

export type KdsTimeInputValue = {
  hours: number;
  minutes: number;
  seconds: number;
  milliseconds: number;
};

export type KdsTimeInputProps = KdsInputFieldProps;
