import { kdsPopoverPlacement } from "../../overlays/Popover";

export const kdsMenuContainerPlacement = kdsPopoverPlacement;

export const kdsMenuContainerPlacements = Object.values(
  kdsMenuContainerPlacement,
);
