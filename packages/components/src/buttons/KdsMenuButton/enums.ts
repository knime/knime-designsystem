import { kdsPopoverPlacement } from "../../overlays/Popover";
import { kdsToggleButtonVariant } from "../KdsToggleButton";

export const kdsMenuButtonVariant = kdsToggleButtonVariant;

export const kdsMenuButtonVariants = Object.values(kdsMenuButtonVariant);

export const kdsMenuButtonPlacement = kdsPopoverPlacement;

export const kdsMenuButtonPlacements = Object.values(kdsMenuButtonPlacement);
