---
"@knime/kds-components": minor
---

Remove the KdsPopover built-in padding and the `paddingSize` prop (including its padding variants). KdsPopover now renders without padding by default; add any required padding via your own content or wrapper element.
